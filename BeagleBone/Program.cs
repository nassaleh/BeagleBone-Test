using System;
using System.Threading;
using BeagleBone;

namespace BBB
{
    class Program
    {
        //static void Main(String[] args)
        //{
        //    int[] pins = { 3, 4, 5, 6 };
        //    //PinManager pm = new PinManager(pins, new PinReader(2), new EFDBHelper());
        //    PinManager pm = new PinManager(pins, new MockReader(), new DBHelper("logbook.db"));

        //    pm.Initialize();

        //    while (true)
        //    {
        //        Thread.Sleep(500);
        //    }
        //}
    }
}